{
  "name": "@cvip/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types, Zod schemas and pure domain logic (pricing, vouchers, state machines).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@babel/runtime": "^7.26.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cvip/config": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
